# 96__pip
# package and modules downloader (used to download and manage packages and modules)
# ex 
# write below
# pip                                               (shows commands)
# pip --version                                     (visar versionen)
# pip install --upgrade pip                         (updaterar pip)
# pip list                                          (lista på alla installerade paket)
    # pip list --outdated                           (visar utdaterade)
        # updatera med pip install namn --upgrade   (för att updatera gamla)
# pip install namn                                  (instalerar nytt paket)

# kan se på Python Package Index (hemsida med alla packet: pypi.org)
